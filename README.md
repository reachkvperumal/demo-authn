# demo-authn
CALM FINOS - Demo project

## View as a GitHub Pages site

This repo includes a Docusaurus site under `docify-out/` and a GitHub Actions workflow to publish it.

1. Push this repo to GitHub (default branch should be `main`).
2. In GitHub: **Settings → Pages**
	- **Build and deployment**: select **GitHub Actions**.
3. Push to `main` (or run the workflow manually).

Your site will be available at:
`https://<github-username-or-org>.github.io/<repo-name>/`
